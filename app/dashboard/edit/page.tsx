import { CanvasEditLayout } from '@/components/dashboard/enterprise/CanvasEditLayout';

/**
 * Vista de edición del lienzo (paso 5): header con undo/redo/Share,
 * canvas con placeholder, panel derecho Visualizations/Data, strip Filters.
 */
export default function EditPage() {
  return <CanvasEditLayout />;
}
